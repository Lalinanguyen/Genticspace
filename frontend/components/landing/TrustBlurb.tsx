export function TrustBlurb() {
  return (
    <div className="px-[5%] pb-[72px] box-border">
      <p className="max-w-[640px] text-[15px] leading-relaxed text-foreground-muted">
        Every listing is independently verified: identity, licensing and deployment claims are
        cross-checked against public agent registries before it goes live, so you never have to
        take a builder&apos;s word for it.
      </p>
    </div>
  );
}
