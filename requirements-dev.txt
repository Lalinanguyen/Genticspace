# Test-only dependencies. Runtime deps (asyncpg, httpx, python-dotenv, ...)
# already live in requirements.txt — install both to run the test suite:
#   pip install -r requirements.txt -r requirements-dev.txt
# See docs/testing.md.
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
